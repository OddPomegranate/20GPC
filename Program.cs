// The real entry point for this project is FiftyGames/Program.cs (it
// already correctly instantiates FiftyGames.FiftyGames via an explicit
// Main method). This file is intentionally left with no code so it does
// not create a second, conflicting program entry point.
